/* CustomTable.css */
const fixedtablediv = {
    maxHeight: "500px",
    overflowY: 'auto'
}
const fixedtableheader = {
    position: 'sticky', top: '0', zIndex: '1'
}
const fixedtablerow = {
    background: '#0b9fc8', color: '#0b9fc8'
}
export { fixedtablediv, fixedtableheader, fixedtablerow };
